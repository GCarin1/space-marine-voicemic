# Dependências de runtime do space-marine.
# Espelha o que está em pyproject.toml [project.dependencies].
# Para instalar em modo "consumir como app":
#   pip install -r requirements.txt
# Para instalar como pacote editável (recomendado):
#   pip install -e .

pedalboard>=0.9.0
sounddevice>=0.4.6
numpy>=1.26
typer>=0.12.0
rich>=13.7
pyyaml>=6.0
soundfile>=0.12.1
